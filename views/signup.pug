extends base
block body
	.container
		.row
			.col-md-5.col-md-offset-3
				form(method="post")
					input(type="hidden", name="_csrf", value=_csrf)
					input.form-control(id="user name", placeholder="Userid", type="text", name="username")
					input.form-control(id="password", placeholder="Password", type="password", name="password")
					input.form-control(id="email", placeholder="Email", type="email", name="email")
					input.form-control(type="submit")